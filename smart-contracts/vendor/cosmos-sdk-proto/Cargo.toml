# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
rust-version = "1.59"
name = "cosmos-sdk-proto"
version = "0.15.0"
authors = ["Justin Kilpatrick <justin@althea.net>", "Greg Szabo <greg@informal.systems>", "Tony Arcieri <tony@iqlusion.io>"]
description = "Protobuf stuct defintions for interacting Cosmos SDK powered blockchains"
readme = "README.md"
keywords = ["blockchain", "cosmos", "tendermint", "proto"]
categories = ["cryptography", "cryptography::cryptocurrencies", "database"]
license = "Apache-2.0"
repository = "https://github.com/cosmos/cosmos-rust/tree/main/cosmos-sdk-proto"
resolver = "2"
[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]
[dependencies.prost]
version = "0.11"

[dependencies.prost-types]
version = "0.11"

[dependencies.tendermint-proto]
version = "0.26"

[dependencies.tonic]
version = "0.8"
features = ["codegen", "prost"]
optional = true
default-features = false

[features]
cosmwasm = []
default = ["grpc-transport"]
grpc = ["tonic"]
grpc-transport = ["grpc", "tonic/transport"]
